healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:11434/"]

# To:
healthcheck:
  test: ["CMD", "ollama", "list"]
  
# Change Qdrant healthcheck from:
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
  
# To:
healthcheck:
  test: ["CMD-SHELL", "wget -q --spider http://localhost:6333/health || exit 1"]
